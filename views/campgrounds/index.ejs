<%- include('../layout/head') %>

  <h1>YelpCamp</h1>

  <ul>
    <% for(let camp of camps) { %>
      <li>
        <b>
          <%= camp.title %>
        </b>
        - <%= camp.location %>
          <a href="/campgrounds/<%= camp._id %>">details</a>
      </li>
      <% } %>
  </ul>


  <%- include('../layout/footer') %>